name: Deploy to ECR

on:
 
  push:
    branches: [ main]

jobs:
  
  build:
    
    name: Build Image
    runs-on: ubuntu-latest

   
    steps:

    - name: Check out code
      uses: actions/checkout@v2
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1


    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: 700740239256.dkr.ecr.ap-south-1.amazonaws.com/nodejsapp:latest
     

    - name: Update ECS task definition
      id: update-task-def
      run: |
          # Run commands to update the task definition
          # For example, using AWS CLI:
          aws ecs register-task-definition \
            --family nodejsapp-td \
            --task-role-arn arn:aws:iam::700740239256:role/ecsTaskExecutionRole \
            --execution-role-arn arn:aws:iam::700740239256:role/ecsTaskExecutionRole \
            --container-definitions file://.github/workflows/task-definition.json

    - name: Deploy Amazon ECS task definition
      uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      with:
        task-definition: ${{ steps.task-def.outputs.task-definition }}
        service: nodejsapp-service
        cluster: nodejsapp
        wait-for-service-stability: true
